# Small script to list status of the clan

## Usage

Get a token on https://developer.clashroyale.com/#/getting-started and put the token
on a file named token. Then run with:

```bash
pipenv install
pipenv shell
python app.py
```
